syntax enable "enables syntax processing (i.e. lets the text be colorful)
set tabstop=4  "number of visual spaces per TAB
set softtabstop=4 "number of spaces in TAB when editing
set shiftwidth=4 
set expandtab "terms tabs into spaces
set number "show the line numbers
set showcmd "show command in bottom bar
set cursorline "highlight the cursor line
set wildmenu "visual autocomplete for command menu
set showmatch "highlight mathing [{()}]
set incsearch "search as charactters are entered
set hlsearch "highlight matches
set colorcolumn=80 "makes a red bar at column 80
set background=dark "sets the background color to dark
set foldenable "enables folding (collasping)
set foldlevelstart=10 "the first ten levels are open by default, 1-99
set foldnestmax=10 "10 nested fold max
set foldmethod=indent "fold based on indent use 'za' to open/close folds
